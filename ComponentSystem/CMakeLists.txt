set(TARGET_NAME ComponentSystem)
set(LINKAGE SHARED)

set(INCLUDED_MODULE_HEADERS ComponentSystem Engine)
set(LINKED_LIBS  )
set(DEPENDS_ON_TARGETS ${LINKED_LIBS} )

include(${MODULE_TEMPLATE_CMAKELISTS_FILE})
