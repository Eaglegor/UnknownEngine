set(TARGET_NAME LogManager)
set(LINKAGE SHARED)

set(INCLUDED_MODULE_HEADERS LogManager Engine Utils)
set(LINKED_LIBS )
set(DEPENDS_ON_TARGETS ${LINKED_LIBS} )

include(${MODULE_TEMPLATE_CMAKELISTS_FILE})