set(TARGET_NAME ResourceManager)
set(LINKAGE SHARED)

set(INCLUDED_MODULE_HEADERS ResourceManager )
set(LINKED_LIBS  )
set(DEPENDS_ON_TARGETS ${LINKED_LIBS} )

include(${MODULE_TEMPLATE_CMAKELISTS_FILE})
