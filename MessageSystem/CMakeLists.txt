set(TARGET_NAME MessageSystem)
set(LINKAGE SHARED)

set(INCLUDED_MODULE_HEADERS MessageSystem Engine Utils LogManager)
set(LINKED_LIBS LogManager )
set(DEPENDS_ON_TARGETS ${LINKED_LIBS} )

include(${MODULE_TEMPLATE_CMAKELISTS_FILE})